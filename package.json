{
  "name": "font-upload",
  "version": "0.1.0",
  "private": true,
  "description": "Orchestrator for adding new fonts across the company's stack.",
  "scripts": {
    "validate": "node scripts/validate-config.js",
    "rollout": "node coordinator/index.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0"
  }
}
