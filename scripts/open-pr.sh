#!/usr/bin/env bash
# Stub — wired up in Phase 4 by the coordinator.
#
# Will: commit the staged changes in the given working directory with a
# templated message, push the branch, and open a PR via `gh pr create`. Prints
# the PR URL to stdout.
set -euo pipefail
echo "open-pr.sh: not implemented yet (Phase 4)" >&2
exit 1
